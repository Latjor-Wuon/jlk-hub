{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bash build.sh",
    "watchPatterns": [
      "backend/**/*.py",
      "frontend/**/*.js",
      "frontend/**/*.html",
      "frontend/**/*.css",
      "requirements.txt",
      "backend/requirements.txt",
      "Procfile",
      "build.sh",
      "runtime.txt",
      "railway.json"
    ]
  },
  "deploy": {
    "startCommand": "if [ -f manage.py ]; then gunicorn jln_hub.wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers 2 --timeout 120 --log-level info; else cd backend && gunicorn jln_hub.wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers 2 --timeout 120 --log-level info; fi",
    "healthcheckPath": "/api/system/health/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}